---
import BaseLayout from '../layouts/BaseLayout.astro';
import { projects } from '../lib/data';

const displayStatus = (status: string, statusLabel?: string) =>
  statusLabel ?? (status === 'LIVE IN PRODUCTION' ? 'Live' : status.toLowerCase());
---

<BaseLayout
  title="Lab · Fran Canete"
  description="Side-project case studies from Fran Canete, including AI SaaS apps Plaudera and Duebase and the engineering lessons behind them."
>
  <main>
    <section id="lab" class="section-hero-personal">
      <div class="container">
        <span class="hero-personal-role">03 / Lab · Side projects</span>

        <h1 class="page-title">What I built, what I <em>learned.</em></h1>

        <div class="page-meta">
          <span class="kind">{String(projects.length).padStart(2, '0')} case studies</span>
          <span class="sep">·</span>
          <span>AI SaaS products</span>
          <span class="sep">·</span>
          <span>Updated Apr 2026</span>
        </div>

        <p class="hero-bio" style="max-width: 720px; margin: 0">
          Two live AI SaaS products, built end to end. These are not tutorials
          or toy apps — they are production systems that taught me how to deal
          with real users, LLM reliability, payments, background jobs, and the
          messy gap between "it works" and "it ships."
        </p>
      </div>
    </section>

    <section class="section">
      <div class="container">
        <div class="lab-list">
          {projects.map((project, index) => {
            const liveUrl = project.links.liveUrl;

            return (
              <article class="lab-case">
                <header class="lab-case-head">
                  <div class="left">
                    <span class="num">{String(index + 1).padStart(2, '0')}</span>
                    <h2>{project.title}.</h2>
                  </div>
                  <div class="right">
                    {liveUrl && (
                      <a href={liveUrl} target="_blank" rel="noopener noreferrer">
                        {new URL(liveUrl).hostname.replace('www.', '')} →
                      </a>
                    )}
                    <span class="sep">·</span>
                    <span>{project.category}</span>
                    <span class="sep">·</span>
                    <span class="status">{displayStatus(project.status, project.statusLabel)}</span>
                  </div>
                </header>
                <p class="lab-lead">{project.summary ?? project.description}</p>

                <div class="lab-detail">
                  <div class="highlights">
                    <span class="label">
                      <span>Highlights</span><span class="count">03 / many</span>
                    </span>
                    <ul>
                      {project.keyFeatures.slice(0, 3).map((feature, featureIndex) => (
                        <li>
                          <span class="n">{String(featureIndex + 1).padStart(2, '0')}</span>
                          <span>{feature}</span>
                        </li>
                      ))}
                    </ul>
                  </div>
                  <div class="stack">
                    <span class="label">
                      <span>Stack</span><span class="count">{project.techStack.length} items</span>
                    </span>
                    <div class="chips">
                      {project.techStack.slice(0, 7).map((technology) => (
                        <span class="chip chip-sm">{technology}</span>
                      ))}
                      {project.techStack.length > 7 && (
                        <span class="mono-text" style="align-self: center; padding-left: 4px">
                          +{project.techStack.length - 7}
                        </span>
                      )}
                    </div>
                  </div>
                </div>

                <div class="lab-learned">
                  <span class="label">What I learned</span>
                  <ul>
                    {project.learned.map((lesson) => (
                      <li>{lesson}</li>
                    ))}
                  </ul>
                </div>
              </article>
            );
          })}
        </div>
      </div>
    </section>
  </main>
</BaseLayout>
